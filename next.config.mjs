/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  async redirects() {
    return [
      // /our-services was renamed to /programs
      {
        source: '/our-services',
        destination: '/programs',
        permanent: true,
      },
      // /incubation-center content merged into /programs
      {
        source: '/incubation-center',
        destination: '/programs',
        permanent: true,
      },
      // /entrepreneurship was an orphaned generic page — redirect home
      {
        source: '/entrepreneurship',
        destination: '/',
        permanent: true,
      },
    ]
  },
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff',
          },
          {
            key: 'X-Frame-Options',
            value: 'DENY',
          },
          {
            key: 'X-XSS-Protection',
            value: '1; mode=block',
          },
          {
            key: 'Referrer-Policy',
            value: 'strict-origin-when-cross-origin',
          },
        ],
      },
    ]
  },
}

export default nextConfig
